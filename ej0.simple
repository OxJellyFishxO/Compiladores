---Sección de declaración de funciones---

def factorial(n) {
  if (n == 0) 
    return 1;
  else
    return n * factorial(n-1);
}

def printString(s) {
    ---
        --Imprime la cadena s
        --Este es un comentario
    ---
}

--- Sección principal ---
def main {
  var n = 5;
  
  printString("Factorial de ");
  printString(n);
  printString(" es ");
  
  var resultado = factorial(n);
  
  printString(resultado);
  
  printString("\n");
}